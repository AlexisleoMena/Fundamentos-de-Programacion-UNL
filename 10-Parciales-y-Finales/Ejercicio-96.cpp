/*
Ejercicio 2 (35pts) Escriba una funcion generica llamada sublistado(..) que reciba un nombre de archivo y un dato X.
La funcion debe cargar los datos del archivo y generar (y retornar) un vector con los elementos menores a X, ordenados
en forma decreciente. 
Utilice la funcion en un programa C++, invocandola y utilizando como parametros de llamada: 
a) una lista de enteros cuyos datos se hallan en el archivo de texto "lista_int.txt" (un entero por linea), 
para obtener los enteros menores a 42; 
b) una lista de strings cuyos datos se hallan en el archivo "lista_str.txt"
(una palabra por linea), para obtener los strings menores a "foo"; 
c) una lista de tipo Evento (struct con dia, mes, 
hora, y descripcion) cuyos datos se hallan en el archivo "eventos.txt" (un evento por linea, los 4 campos separados
por espacios), para obtener los eventos previos al 4 de mayo de 2021.
*/
#include <iostream>
using namespace std;

int main(int argc, char *argv[]) {
	
	return 0;
}

