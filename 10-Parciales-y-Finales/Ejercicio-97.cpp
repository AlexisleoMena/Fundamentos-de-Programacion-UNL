/*
Ejercicio 3 (30pts) Realice un programa que lea archivo de texto llamado "email.txt" que contiene una lista de
correos y lo analice, contando cuantas veces se repite cada dominio. Debe generar otro archivo de texto 
"estadasticas.txt", en el cual cada fila contenga el nombre de dominio y la cantidad de correos que utilizan dicho
dominio; estas lineas deben estar ordenadas en forma descendente por cantidad. (Nota: el dominio es la la parte de 
la direccion de correo que se encuentra luego de la '@')
*/
#include <iostream>
using namespace std;

int main(int argc, char *argv[]) {
	
	return 0;
}

